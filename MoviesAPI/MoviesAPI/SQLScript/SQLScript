create database Movies;
use Movies;

create table Movie(
IdMovie int not null primary key,
Title varchar(255),
ReleaseYear varchar(10),
Genre varchar(255),
Director varchar(255),
Rate int,
MovieUrl varchar(2000),
TrailerUrl varchar(255)
);